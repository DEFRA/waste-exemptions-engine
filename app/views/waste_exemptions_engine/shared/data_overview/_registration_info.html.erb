<h2 class="govuk-heading-m">
  <%= t(".subheading") %>
</h2>

<span class="govuk-visually-hidden">
  <%= t(".table_summary") %>
</span>

<dl class="govuk-summary-list govuk-!-margin-bottom-9">
<% form.registration_rows.each do |row| %>
  <% no_border_css = row[:no_border] ? "govuk-summary-list__row--no-border" : "" %>
  <div class="govuk-summary-list__row <%= no_border_css %>">
    <dt class="govuk-summary-list__key">
      <%= row[:title] %>
    </dt>
    <dd id="<%= html_id_for("registration", row[:title]) %>" class="govuk-summary-list__value">
      <%= row[:value] %>
    </dd>
    <dd class="govuk-summary-list__actions">
      <% if row[:change_url].present? %>
        <%= link_to(row[:change_url]) do %>
          <%= t(".change_link")%>
          <span class="govuk-visually-hidden">
            <%= row[:change_link_suffix] %>
          </span>
        <% end %>
      <% end %>
    </dd>
  </div>
<% end %>
</dl>
