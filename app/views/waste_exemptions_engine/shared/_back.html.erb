<%
  content_for :back_link do
    link_to(
        defined?(back_link_label) ? back_link_label : t(".back_link"),
        defined?(back_path) ? back_path : go_back_forms_path(token),
        class: "govuk-back-link"
      )
  end
%>
