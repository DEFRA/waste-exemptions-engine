<%
  content_for :back_link do
    link_to(
        local_assigns[:back_link_label].present? ? local_assigns[:back_link_label] : t(".back_link"),
        local_assigns[:back_path].present? ? local_assigns[:back_path] : go_back_forms_path(token),
        class: "govuk-back-link"
      )
  end
%>

<%= render partial: "waste_exemptions_engine/shared/notification_banner" %>
