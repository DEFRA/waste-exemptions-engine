<%= render("waste_exemptions_engine/shared/back", back_path: back_confirm_edit_cancelled_forms_path(@confirm_edit_cancelled_form.token)) %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <%= form_for(@confirm_edit_cancelled_form) do |f| %>
      <%= render partial: "waste_exemptions_engine/shared/error_summary", locals: { f: f } %>

      <h1 class="govuk-heading-l"><%= t(".heading") %></h1>

      <p class="govuk-body"><%= t(".paragraph_1") %></p>

      <%= f.govuk_submit t(".next_button") %>
    <% end %>
  </div>
</div>
