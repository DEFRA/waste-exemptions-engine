<%= render("waste_exemptions_engine/shared/back", back_path: back_confirm_edit_cancelled_forms_path(@confirm_edit_cancelled_form.token)) %>

<div class="text">
  <%= form_for(@confirm_edit_cancelled_form) do |f| %>
    <%= render("waste_exemptions_engine/shared/errors", object: @confirm_edit_cancelled_form) %>

    <h1 class="heading-large"><%= t(".heading") %></h1>

    <p><%= t(".paragraph_1") %></p>

    <%= f.hidden_field :token, value: @confirm_edit_cancelled_form.token %>
    <div class="form-group">
      <%= f.submit t(".next_button"), class: "button" %>
    </div>
  <% end %>
</div>
