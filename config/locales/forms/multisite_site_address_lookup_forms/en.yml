en:
  waste_exemptions_engine:
    multisite_site_address_lookup_forms:
      new:
        title: Select the address of the waste operation
        heading: Select the address of the waste operation
        postcode_label: "Postcode:"
        postcode_change_link: Change
        grid_reference_link: Use grid reference instead
        error_heading: A problem to fix
        next_button: Continue

  activemodel:
    errors:
      models:
        waste_exemptions_engine/multisite_site_address_lookup_form:
          attributes:
            temp_site_address:
              blank: Select an address
              invalid: Select a valid address
            token:
              invalid_format: The token is not valid
              missing: The token is missing
